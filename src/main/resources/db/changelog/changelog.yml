databaseChangeLog:
  - changeSet:
      id: 1
      author: your_name
      changes:
        - sql:
            sql: |
              CREATE TABLE reminder_table (
              id INT PRIMARY KEY,
              name VARCHAR(50) NOT NULL
              );