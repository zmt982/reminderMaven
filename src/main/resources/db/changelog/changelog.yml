databaseChangeLog:
  - changeSet:
      id: 1
      author: your_name
      changes:
        - sql:
            sql: |
              CREATE TABLE user (
              id INT PRIMARY KEY,
              username VARCHAR(50) NOT NULL,
              email
              );